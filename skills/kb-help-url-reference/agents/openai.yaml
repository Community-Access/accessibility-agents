# Codex interface for this skill.
# Reference skills are lookup data cited by other skills, never chosen by the
# model on its own. Without this file Codex lists them in its catalog, which a
# live probe showed put all 29 into the model prompt.
interface:
  display_name: "Help Url Reference"
  short_description: "Reference data, not a reviewer. Map accessibility findings to help resources: axe-core to Accessibility Insights, document rules to Microsoft Office/Adobe, WCAG criteria to W3C Understanding documents."
policy:
  allow_implicit_invocation: false
